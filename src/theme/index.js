import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#171A21",
            contrastText: "#ffffff",
            
        }
    }
});

export default theme;